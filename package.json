{
  "name": "drizzle",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "db:generate": "bun --bun run drizzle-kit generate --config=./config/drizzle.config.ts",
    "db:migrate": "bun --bun run drizzle-kit migrate --config=./config/drizzle.config.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.5",
    "drizzle-kit": "^1.0.0-beta.6-4414a19"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "drizzle-orm": "^1.0.0-beta.6-4414a19"
  }
}
